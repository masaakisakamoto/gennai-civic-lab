# gennai-local-runner

Planned local UI/mock runner for Gennai-compatible apps.

Current v0.2 scope is a tiny CLI smoke runner. Future scope:

- load manifest JSON
- render a local form
- call an app endpoint
- show `outputs` Markdown
- export curl examples
