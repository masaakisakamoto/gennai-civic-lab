[project]
name = "gennai-app-kit"
version = "0.7.0"
description = "SDK for Gennai-compatible civic AI apps"
requires-python = ">=3.11"
readme = "README.md"
authors = [{name = "gennai-civic-lab contributors"}]
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
