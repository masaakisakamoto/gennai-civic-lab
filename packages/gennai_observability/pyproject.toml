[project]
name = "gennai-observability"
version = "0.6.0"
description = "PII-conscious tracing, metrics, and operational reporting for Gennai-compatible apps"
requires-python = ">=3.11"
dependencies = []
readme = "README.md"
license = {text = "MIT"}

[project.scripts]
gennai-ops-report = "gennai_observability.ops_report:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
